# [job-queue] is required, not optional: it supplies the scheduler that
# retries an order cycle once the Israeli exit node comes back online.
# Without the extra, telegram.ext.JobQueue still imports but
# Application.job_queue is None and the retry silently never runs.
python-telegram-bot[job-queue]>=21.0,<22
playwright>=1.45,<2
PyYAML>=6.0,<7
# Tiv Taam talks JSON over the Israeli SOCKS exit; socksio is what
# gives httpx socks5h:// support (DNS resolved at the exit, not here).
httpx>=0.27,<1
socksio>=1.0,<2
